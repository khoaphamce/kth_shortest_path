#include <iostream>
#include <iomanip>
#include <math.h>
#include <bits/stdc++.h>
#include "math_algo.h"
#include "data_structure.h"
#include "algo.h"
#include "io_func.h"


int main(){
    // data_structure_h::graph inputGraph = io_func_h::input_graph();
    // std::cout << "Graph: " << std::endl;

    // inputGraph.print();

    // std::vector<std::vector<long int>> mandEdges = io_func_h::input_mand_edges();
    // std::vector<std::vector<std::vector<long int>>> perm_mandEdges = algo_h::edges_order_perm(mandEdges);
    // std::cout << std::endl << "Permutation of edges:" << std::endl << std::endl;
    // for(int i = 0; i < perm_mandEdges.size(); i++){
    //     io_func_h::display_edges(perm_mandEdges[i]);
    //     std::cout << std::endl;
    // }

    // std::cout << std::endl;

    return 0;
}